# Document Automation Workspace 인수인계

작성일: 2026-05-25
브랜치: `main`
원격 저장소: `origin https://github.com/CodeofO/document-automation-workspace.git`

## 0. 2026-05-25 최신 상태

이번 세션에서 큰 구조 변경을 진행했다. 기존 “각 모듈이 직접 업로드하고 바로 전처리”하는 흐름을 기본 경로에서 밀어내고, `문서 보관함`을 공통 입력 계층으로 세웠다.

핵심 완료 사항:

- 홈에 `문서 보관함` 진입점 추가
- `frontend/src/DocumentLibrary.tsx` 추가
  - 파일/폴더 선택
  - drag/drop
  - 업로드 대기열
  - 폴더 트리
  - 빈 폴더 생성
  - 전체 선택
  - 파일/폴더 복사
  - 파일/폴더 이동
  - 잘라내기/붙여넣기
  - 선택 삭제
  - `Command/Ctrl+A/C/X/V`, `Delete` 키보드 단축키
  - 상태 필터/검색
  - 원본 삭제
- `Document.library_path`, `Document.deleted_at`, `DocumentConversionJob`, `DocumentLibraryFolder` 추가
- `/api/library/uploads`, `/api/library/tree`, `/api/library/folders`, `/api/library/copy`, `/api/library/move`, `GET /api/documents/ids`, `POST /api/documents/selection`, `POST /api/documents/delete`, `DELETE /api/documents/{id}` 추가
- KIE/classifier/required/workflow에 `from-documents` API 추가
- `queued/preprocessing` 문서를 실행 대상으로 넣으면 `waiting_for_document`로 등록하고, 변환 완료 후 자동 실행
- 워크플로우/모듈 신규 업로드도 보관함에 먼저 저장한 뒤 document id 기준으로 실행
- 워크플로우 `discard`는 더 이상 보관함 문서를 삭제하지 않고 추론만 중단한다.
- 워크플로우 빌더의 `업로드 삭제` 표현은 `추론 중단`/`시작 중단`으로 정리했다.
- `/api/documents` 단일 업로드는 기존 회귀 테스트와 호환성을 위해 즉시 전처리 완료 계약 유지
- `WORKFLOW_MAX_WORKERS`와 `VLM_MAX_CONCURRENT_REQUESTS` 분리
- Gemini/OpenAI-compatible/mock VLM 호출에 async 경로와 AI 동시 요청 semaphore 추가

최근 검증:

```bash
npm run build --prefix frontend
.venv/bin/python -m compileall backend/app
.venv/bin/pytest backend/tests/test_api.py -q
git diff --check
```

결과:

- frontend build 통과
- backend compile 통과
- backend API test `92 passed`
- whitespace error 없음

개발 서버 상태:

- `127.0.0.1:5173` frontend dev server 리슨 중
- `127.0.0.1:8000` backend dev server 리슨 중

주의:

- 실제 VLM 대량 smoke는 과금/사용자 실행 작업 영향 때문에 자동 실행하지 않았다.
- 기존 `init -> items -> start` multipart API는 호환/이어가기용으로 남아 있다. 신규 UI 기본 경로는 보관함이다.
- 보관함 전환은 1차 구현이다. 상세 E2E 브라우저 검증은 후속으로 폴더 업로드, 변환 중 예약, ready 후 자동 실행까지 실제 샘플로 확인해야 한다.

## 0.1 2026-05-25 완료: 문서 보관함 파일 관리자 기능 추가

완료한 신규 업무:

1. 문서 보관함에 `전체 선택` 기능을 추가한다.
2. 업로드된 문서를 실제 파일 관리 시스템처럼 다룰 수 있게 한다.
   - 파일 복사
   - 파일 이동
   - 폴더 복사
   - 폴더 이동
   - 복사/잘라내기/붙여넣기
   - 폴더 내 자유로운 정리

구현 파일:

- `backend/app/models.py`: `DocumentLibraryFolder`
- `backend/app/schemas.py`: 보관함 폴더/clipboard action schema
- `backend/app/main.py`: 폴더 생성, 전체 id 조회, 선택 문서 조회, copy/move API, workflow discard 의미 변경
- `frontend/src/DocumentLibrary.tsx`: 전체 선택, 복사/이동 clipboard, 붙여넣기, 새 폴더, 현재 폴더 복사/이동
- `frontend/src/DocumentLibrary.tsx`: 선택 삭제와 `Command/Ctrl+A/C/X/V`, `Delete` 단축키 추가
- `frontend/src/WorkflowBuilder.tsx`: 문서 보관함 기준으로 중단/재실행 버튼 문구 정리
- `frontend/src/styles.css`: 보관함 folder tool 스타일
- `backend/tests/test_api.py`: 문서/폴더 copy/move 및 workflow discard 회귀 테스트

### 현재 보관함 구조

- 문서 보관함은 `Document.library_path` 문자열을 기준으로 폴더처럼 보여준다.
- 빈 폴더는 `DocumentLibraryFolder.path`에 명시적으로 저장한다.
- `/api/library/tree`는 explicit folder와 `documents.library_path`에서 유도한 폴더를 합쳐 반환한다.
- `DELETE /api/documents/{document_id}`는 원본 payload와 page image를 삭제하고 문서를 `deleted` 상태로 바꾼다.
- 업로드된 원본/페이지 파일은 `Document.storage_path`, `DocumentPage.image_path`로 관리된다.
- 저장소 helper는 `backend/app/storage.py`에 있다.
  - `materialize_storage_ref`
  - `persist_artifact`
  - `delete_storage_ref`
  - `is_s3_ref`
- 로컬 저장소는 문서별 UUID 폴더에 원본과 page artifact를 둔다.
- S3 backend도 고려되어 있으므로 복사 구현은 로컬 path만 직접 조작하지 말고 기존 storage helper를 경유하는 쪽이 안전하다.

### 백엔드 구현 요약

추가 모델:

```python
class DocumentLibraryFolder(Base):
    __tablename__ = "document_library_folders"

    id: Mapped[str] = mapped_column(String, primary_key=True, default=lambda: new_id("library_folder"))
    path: Mapped[str] = mapped_column(String, nullable=False, unique=True)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), nullable=False)
```

주의:

- `Base.metadata.create_all()` 때문에 새 테이블은 신규 DB에서 자동 생성된다.
- 기존 SQLite에는 `create_all`이 새 테이블을 만들 수 있으므로 column migration보다 부담이 작다.
- path는 항상 `_normalize_library_path()`를 통과시킨다.
- 루트 폴더 `""`는 테이블에 넣지 않아도 된다.

추가 schema:

```python
class DocumentLibraryCreateFolderRequest(BaseModel):
    folder_path: str = Field(min_length=1, max_length=1000)

class DocumentLibraryClipboardRequest(BaseModel):
    document_ids: list[str] = Field(default_factory=list, max_length=10000)
    folder_paths: list[str] = Field(default_factory=list, max_length=1000)
    target_folder: str = Field(default="", max_length=1000)

```

검증 규칙:

- `document_ids`와 `folder_paths` 둘 다 비어 있으면 400.
- `target_folder`는 정규화한다.
- 자기 자신 안으로 폴더 이동/복사는 거부한다.
  - 예: `A`를 `A/B`로 이동 금지
- `deleted` 문서는 복사/이동 대상에서 제외하거나 409로 거부한다.
- `failed` 문서는 UI 선택 대상에서 이미 제외되어 있지만 API에서도 명시적으로 거부하는 편이 안전하다.

추가 API:

- `POST /api/library/folders`
  - 빈 폴더 생성
  - 이미 있으면 idempotent하게 성공 처리 가능
- `POST /api/library/move`
  - 선택 문서와 선택 폴더를 target folder로 이동
  - 문서는 `Document.library_path` 변경
  - 폴더는 해당 prefix의 모든 문서와 explicit folder path를 함께 변경
- `POST /api/library/copy`
  - 선택 문서와 선택 폴더를 target folder로 복사
  - 새 `Document` row와 `DocumentPage` row 생성
  - 원본/payload artifact도 새 문서 storage 위치로 복제

복사 구현 원칙:

- 같은 `Document.storage_path`를 공유하지 않는다.
- 새 문서는 새 id와 새 storage directory를 가져야 한다.
- 원본 파일과 페이지 이미지도 새 위치에 복사한다.
- 이유: 원본 삭제 시 공유 payload가 같이 사라지는 문제가 생기면 안 된다.
- S3 backend까지 고려하려면 다음 흐름을 사용한다.
  1. `materialize_storage_ref(source_ref)`로 읽을 수 있는 로컬 path 확보
  2. 새 storage scratch/local path에 `shutil.copy2`
  3. `persist_artifact(new_path, f"documents/{new_document.id}/...")`
  4. DB에는 `persist_artifact` 결과 ref 저장

경로 충돌 처리:

- 같은 target folder에 같은 파일명이 있으면 자동으로 고유 이름을 만든다.
- 예:
  - `bank_25125.jpg`
  - `bank_25125 copy.jpg`
  - `bank_25125 copy 2.jpg`
- 폴더 복사 충돌도 같은 방식으로 처리한다.
  - `검수`
  - `검수 copy`
  - `검수 copy 2`
- 이동 충돌도 자동 rename이 사용자 경험상 가장 덜 막힌다.

`/api/library/tree`:

- `DocumentLibraryFolder` explicit folder와 document-derived folder를 합쳐 반환한다.
- 빈 폴더도 사이드바에 표시된다.
- 폴더 count는 해당 폴더 하위 전체 document 기준으로 집계한다.

### 프론트엔드 구현 요약

- `DocumentLibraryPanel`이 보관함 화면과 picker modal을 모두 담당한다.
- `GET /api/documents/ids`로 현재 필터/폴더 기준 전체 선택 id를 받아온다.
- 선택 id가 현재 화면에 로드되지 않은 경우 `POST /api/documents/selection`으로 실행 직전 문서 객체를 보충한다.
- 선택 바에 `전체 선택`, `선택 해제`, `복사`, `이동`, `붙여넣기`를 추가했다.
- 폴더 영역에 `새 폴더`, `폴더 복사`, `폴더 이동`, `붙여넣기`를 추가했다.
- 업로드는 현재 선택한 폴더 안으로 들어가도록 `uploadLibraryFiles(files, targetFolder)`를 지원한다.

clipboard state:

```ts
type LibraryClipboard = {
  mode: "copy" | "cut";
  documentIds: string[];
  folderPaths: string[];
} | null;
```

동작:

- 파일 복사:
  1. 문서 선택
  2. `복사`
  3. 대상 폴더 클릭
  4. `붙여넣기`
  5. `/api/library/copy`
- 파일 이동:
  1. 문서 선택
  2. `잘라내기`
  3. 대상 폴더 클릭
  4. `붙여넣기`
  5. `/api/library/move`
- 폴더 복사/이동:
  1. 폴더 선택
  2. `현재 폴더 복사` 또는 `현재 폴더 잘라내기`
  3. 대상 폴더 클릭
  4. `붙여넣기`
- 붙여넣기 성공 후:
  - `refreshLibrary({ silent: true })`
  - 복사면 clipboard 유지 또는 해제 중 UX 선택. 일반 파일 관리자는 복사 clipboard 유지가 자연스럽다.
  - 잘라내기면 clipboard 해제.

선택 모델:

- 전체 선택은 현재 화면에 로드되지 않은 id도 포함할 수 있다.
- 실행/선택 적용 시 `POST /api/documents/selection`으로 필요한 문서 객체를 다시 조회한다.

### 테스트 완료 항목

Backend:

- 폴더 생성 후 `/api/library/tree`에 빈 폴더가 표시되는지 확인
- 문서 1개 이동 시 `library_path`가 target folder로 바뀌는지 확인
- 문서 1개 복사 시:
  - 새 `Document`가 생성되는지
  - 새 document id인지
  - 새 `storage_path`인지
  - page row도 복제되는지
  - 원본 삭제 후 복사본 조회가 유지되는지
- 폴더 이동 시 prefix 하위 문서가 모두 이동되는지 확인
- 폴더 복사 시 하위 문서가 모두 새 prefix로 복사되는지 확인
- 같은 폴더에 복사할 때 이름 충돌이 자동 rename되는지 확인
- deleted/failed 문서 복사/이동 거부 확인

Frontend:

- `전체 선택`/`선택 해제` 동작
- 문서 복사 후 다른 폴더에 붙여넣기
- 문서 잘라내기 후 다른 폴더에 붙여넣기
- 현재 폴더 복사 후 target folder에 붙여넣기
- 현재 폴더 잘라내기 후 target folder에 붙여넣기
- 빈 폴더 생성 후 사이드바 표시
- 업로드 대기 중에도 기존 보관함 조작 버튼이 깨지지 않는지 확인

검증 명령:

```bash
npm run build --prefix frontend
.venv/bin/python -m compileall backend/app
.venv/bin/pytest backend/tests/test_api.py -q
git diff --check
```

검증 완료:

```bash
npm run build --prefix frontend
.venv/bin/python -m compileall backend/app
.venv/bin/pytest backend/tests/test_api.py -q
```

결과:

- frontend build 통과
- backend compile 통과
- backend API test `92 passed`

## 1. 현재 목표

문서 접수, 문서 보관함, 원문 변환, 문서 분류, 필수 항목 확인, 핵심 정보 추출, 워크플로우 실행, CSV/JSON/XLSX export를 하나의 작업 공간에서 처리하는 문서 자동화 워크스페이스다.

최근 작업의 중심은 다음 세 가지다.

- 워크플로우 빌더 UX 정리
- 대용량 업로드 안정화
- 홈 화면과 결과 확인 화면의 반응형/가독성 보강

## 2. 이번 작업 완료 내용

### 홈 화면

- 상단 배지는 `VLM · model name` 형태로 정리했다.
- workers 표시는 홈에서 제거했다.
- `워크플로우 빌더 열기` 버튼을 primary CTA로 키웠다.
- 핵심 가치 카드 3개를 홈 hero 안으로 정리했다.
- 진행 현황 패널을 홈 hero 우측에 배치했다.
- 진행 현황은 클릭 이동이 아니라 보기 전용으로 바꿨다.
- 진행 현황 각 실행 카드 안에 progress bar를 넣었다.
- 상태 뱃지와 별도 하단 progress는 제거했다.
- 좁은 해상도에서 가치 카드 텍스트가 박스 밖으로 밀리는 문제를 수정했다.
- 1280, 900, 640 폭에서 카드가 부모 영역 밖으로 나가지 않는 것을 브라우저로 확인했다.

### 워크플로우 빌더

- 우측 설정 사이드바를 제거했다.
- classifier, KIE, required-checker 노드 내부에 설정 select를 직접 넣었다.
- branch 노드의 route 항목을 좌측 정렬했다.
- 실행 중에는 캔버스 연결선이 점선으로 흐르는 애니메이션을 가진다.
- 논리 오류가 아닌 다중 연결 확인 팝업은 제거했다.
- `결과 상세보기` 버튼은 캔버스 진행 dock 쪽 1개만 남겼다.
- 결과 상세보기는 팝업이 아니라 앱 내부 화면 전환 방식으로 변경했다.
- 결과 화면에서 CSV/JSON export 버튼을 바로 볼 수 있다.
- 워크플로우 빌더 내부에 `실행 기록` 패널을 추가했다.
- 실행 기록에서 과거 run의 `결과 보기`, `CSV`, `JSON`에 접근할 수 있다.

### 워크플로우 대용량 업로드

기존 문제:

- 5000장 이상을 한 번의 multipart 요청으로 보내면 Starlette multipart parser가 임시 파일을 대량으로 열고 `OSError: [Errno 24] Too many open files`가 발생했다.

변경:

- 워크플로우 실행은 이제 한 번에 모든 파일을 보내지 않는다.
- 프론트가 파일을 `100개 단위`로 나눠 업로드한다.
- 백엔드는 다음 API 흐름으로 하나의 workflow run을 구성한다.
  - `POST /api/workflows/{workflow_id}/runs/init`
  - `POST /api/workflow-runs/{run_id}/items`
  - `POST /api/workflow-runs/{run_id}/start`
- 업로드 중에는 상단 진행 dock에 `0 / 5000 문서 업로드 중` 같은 상태가 표시된다.
- 백엔드는 업로드 파일 처리 후 `UploadFile.close()`를 호출해 파일 핸들을 닫도록 보강했다.
- multipart 파싱 중 `EMFILE`이 발생하면 500 대신 413과 명확한 메시지를 반환한다.

### KIE / 문서 분류 / 필수 항목 확인 대용량 업로드

- KIE batch, classifier batch, required-checker batch도 프론트에서 `100개 단위`로 쪼개 여러 배치를 생성한다.
- 기존 batch API는 유지했다.
- 한 번의 요청에 수천 개 파일이 들어가지 않게 해 파일 핸들 폭증을 피한다.
- 여러 배치로 나뉜 경우 사용자 메시지에 몇 개 배치로 나눠 시작했는지 표시한다.

### VLM 안정화

- Google GenAI 호출에서 `Broken pipe`, timeout, 429, 5xx 등 transient 오류는 제한적으로 retry한다.
- 무효 요청, schema 오류처럼 재시도해도 의미 없는 오류는 즉시 실패하도록 유지했다.
- 관련 회귀 테스트를 추가했다.

## 3. 현재 검증 상태

최근 통과한 검증:

```bash
cd frontend
npm run build
```

```bash
.venv/bin/python -m pytest backend/tests/test_api.py -q
```

결과:

- frontend build 통과
- backend API test `92 passed`
- `git diff --check` 통과
- Browser responsive check 통과: 1280, 900, 640 폭에서 홈 가치 카드 overflow 없음

주의:

- Vite build에서 chunk size warning이 계속 나온다. 기능 실패는 아니며, 추후 code splitting 후보로 보면 된다.
- 실제 VLM 호출은 과금 가능성이 있으므로 사용자의 명시 승인 없이 smoke test하지 않는다.

## 4. 주요 변경 파일

Backend:

- `backend/app/main.py`
  - workflow chunk upload API 추가
  - upload file close 보강
  - EMFILE 처리 보강
- `backend/app/schemas.py`
  - `WorkflowRunInitRequest` 추가
- `backend/app/vlm.py`
  - transient VLM provider failure retry 추가
- `backend/tests/test_api.py`
  - VLM Broken pipe retry 테스트 추가

Frontend:

- `frontend/src/App.tsx`
  - 홈 진행 현황
  - KIE batch chunk upload
  - 홈 반응형 구조
- `frontend/src/ModuleWorkspace.tsx`
  - classifier/required batch chunk upload
- `frontend/src/WorkflowBuilder.tsx`
  - node 내부 설정 select
  - workflow chunk upload
  - 실행 기록 패널
  - 결과 화면 전환
- `frontend/src/styles.css`
  - 홈/진행현황/워크플로우 결과/실행기록 레이아웃 보강

## 5. 현재 동작 흐름

### 워크플로우 실행

1. 사용자가 파일을 선택한다.
2. 실행 버튼을 누른다.
3. 프론트가 workflow를 저장한다.
4. `runs/init`으로 빈 run을 만든다.
5. 파일을 100개씩 `workflow-runs/{run_id}/items`로 업로드한다.
6. 모든 파일이 올라가면 `workflow-runs/{run_id}/start`를 호출한다.
7. 백엔드 background task가 기존 workflow engine으로 실행한다.
8. 결과는 `워크플로우 실행 결과` 화면 또는 `실행 기록`에서 확인한다.

### KIE / Classifier / Required batch

1. 사용자가 파일을 여러 개 선택한다.
2. 프론트가 100개 단위로 나눈다.
3. 기존 batch 생성 API를 여러 번 호출한다.
4. 각 batch는 기존 background worker로 병렬 처리된다.
5. 첫 번째 batch를 active batch로 보여준다.

## 6. 남은 개선 후보

우선순위 높음:

- 5000장 실제 샘플로 로컬 end-to-end smoke test
- 대용량 batch 전체를 하나의 상위 그룹으로 묶어 보여주는 UI
- workflow run cancel API
- 업로드 중 취소 기능
- workflow/result polling을 SSE 또는 WebSocket으로 개선
- 대용량 업로드 chunk size를 `.env` 또는 frontend config로 조정 가능하게 만들기

중간 우선순위:

- frontend code splitting
- workflow 실행 기록 필터/검색
- batch export를 여러 batch 묶음으로 한 번에 export
- XLSX export

## 7. 작업 시 주의사항

- 사용자가 만든 변경을 되돌리지 않는다.
- 실제 VLM 호출 전에는 반드시 사용자에게 묻는다.
- `.gitignore`는 현재 전체 ignore 후 필요한 파일만 예외 처리하는 구조다.
- `인수인계.md`, `DEVELOPMENT_DEFINITION.md`, `ERROR_NOTE.md` 같은 개발 문서는 현재 git 추적 대상이 아니다.
- 로컬 개발은 기존처럼 `.env`, SQLite, filesystem 저장소로 동작한다.
- 외부 호스팅은 env, CORS, shared access code, storage backend 준비가 되어 있지만 현재 운영 테스트는 별도 확인이 필요하다.

---

## 8. 2026-05-24 최신 인수인계

이번 세션에서는 대용량 실행 안정화, 워크플로우 재실행/중단/이어가기, export, 결과 검수 UI, KIE AI 검수 기능, 프롬프트 계층 분리까지 넓게 수정했다. 아래 내용은 최신 코드 상태 기준이다.

### 8.1 대용량 업로드/실행 공통 안정화

- 워크플로우, KIE batch, 문서 분류 batch, 필수 항목 확인 batch의 업로드 흐름을 `init -> items -> start` 구조로 확장했다.
- 기존 단일 multipart API는 호환성용으로 유지하되 내부 동작은 새 흐름에 맞춰 보강했다.
- 업로드 chunk size를 환경값으로 제어하도록 했다.
  - `UPLOAD_CHUNK_FILES`
  - 기본값은 안정성 우선으로 낮게 잡는다.
- 전처리 worker 수와 문서 preview/VLM 입력 이미지 정책을 설정값으로 분리했다.
  - `PREPROCESS_MAX_WORKERS`
  - `DOCUMENT_PAGE_MAX_LONG_EDGE`
  - `DOCUMENT_PAGE_JPEG_QUALITY`
- `/api/system/status`에서 업로드/전처리 관련 설정값을 내려주고 frontend는 하드코딩된 chunk 크기를 쓰지 않도록 정리했다.
- 파일 1개 처리 후 가능한 빨리 DB commit되도록 하여, 대량 업로드 중 일부 실패가 나도 이전 성공분은 UI에 반영되게 했다.
- `client_file_id`를 사용해 같은 owner 안에서 중복 업로드를 idempotent하게 처리한다.
- 업로드 실패로 총 업로드 수가 부족한 경우도, 실패 item이 명시적으로 생성되어 있으면 “업로드 완료”로 보고 실행 가능하게 보강했다.
- 이미지 pixel count 초과 방지를 위해 preview/VLM용 이미지를 최대 픽셀 수 이하로 비율 축소하는 로직을 추가했다.
  - 경계값을 아주 조금 넘는 케이스도 막기 위해 보정 margin을 둔다.

### 8.2 병렬 처리 및 DB pool 안정화

- 문서 처리 작업 수는 `WORKFLOW_MAX_WORKERS`로 관리한다.
- provider로 나가는 AI 동시 요청 수는 `VLM_MAX_CONCURRENT_REQUESTS`로 별도 관리한다.
- 홈 설정 화면에서 두 값을 각각 저장할 수 있다.
- 워크플로우/KIE 내부 VLM 호출은 async 경로와 semaphore로 실제 AI in-flight 수를 제한한다.
- DB connection pool 설정을 늘려 대량 병렬 실행 중 `QueuePool limit of size 5 overflow 10 reached`가 쉽게 터지지 않도록 했다.
- 홈 설정 화면에 VLM 동시 요청 수 안내 문구를 추가했다.
  - 너무 높이면 처리량은 늘 수 있지만 DB connection과 VLM rate limit 부담도 같이 증가한다는 내용이다.

### 8.3 워크플로우 중단/이어가기/재시작

- 워크플로우 run에 다음 동작을 정리했다.
  - `일시중단`: 업로드 중이거나 추론 중인 run을 멈춘다. 업로드된 문서는 유지한다.
  - `이어가기`: 일시중단된 run을 다시 실행한다.
  - `재시작`: 업로드 문서는 재사용하고, 기존 추론 결과는 새 run에서 다시 만든다.
  - `중단·정리`: 시도 기록은 남기되 업로드 산출물을 정리한다.
- 재시작은 5000개 파일을 물리 복사하지 않는다.
  - 기존 `Document`와 page artifact를 재사용하고 새 `WorkflowRun`/`WorkflowRunItem`만 만든다.
- 재시작 시 기존 run은 history/export 가능 상태로 남고, 새 run이 별도 job처럼 history에 남는다.
- 실행 중 frontend 새로고침으로 업로드가 끊긴 경우에도 전체 파일을 다시 선택해 남은 파일만 이어 업로드할 수 있게 했다.
- 파일 업로드/폴더 업로드 버튼은 `업로드` 하나로 통합했고, 파일 이어가기/폴더 이어가기도 `이어가기` 하나로 통합했다.

### 8.4 워크플로우 실행 이력과 export

- workflow run에 실행 당시 workflow snapshot을 저장한다.
  - `workflow_name`
  - `workflow_definition_json`
  - `restarted_from_run_id`
- run 실행 시 현재 workflow definition이 아니라 run에 저장된 snapshot을 사용한다.
  - 실험1/2/3/4처럼 workflow를 바꿔가며 재실행할 때 결과가 이전 workflow와 섞이지 않게 하기 위한 핵심 수정이다.
- export 파일명은 job id만 쓰지 않고 모듈/스키마 또는 workflow명까지 포함하도록 바꿨다.
  - 예: `{module}_{schema_or_workflow_name}_{job_id}.csv`
- workflow HISTORY에 어떤 workflow로 실행했는지 표시한다.
- workflow export와 KIE export에 AI 검수 메타데이터를 포함한다.
  - 기본 최종값 컬럼: `kie_{field}`
  - 1차 추론값: `kie_{field}_ai_initial_value`
  - AI 검수 여부: `kie_{field}_ai_review_enabled`
  - AI 검수 판단: `kie_{field}_ai_review_status`
  - AI 보정 여부: `kie_{field}_ai_corrected`
  - AI 판단/보정 이유: `kie_{field}_ai_review_reason`, `kie_{field}_ai_correction_reason`
  - 수동 검수 관련: `kie_{field}_reviewed`, `kie_{field}_changed`, `kie_{field}_original`, `kie_{field}_warnings`
- 사용자가 제공한 실험 CSV에서 최종 반영값은 접미사가 없는 `kie_본인성명`, `kie_법정대리인성명` 컬럼임을 확인했다.

### 8.5 워크플로우 결과 상세 화면

- 결과 상세 화면을 크게 세 영역으로 정리했다.
  - 좌: 대상 문서 목록
  - 중: 이미지 미리보기
  - 우: 결과
- KIE 스타일을 참고해 영역 사이 너비 조절 핸들을 추가했다.
- 결과 목록 필터를 추가했다.
  - 성공
  - 실패
  - 대기
  - 실행
  - 검토
  - 클래스별 필터
  - 중복 조건 필터
  - AI 보정됨
  - AI 검수 실패
- polling은 summary endpoint를 사용하고, 상세 화면에서만 item page를 가져오도록 정리했다.
- 업로드/실행 중 결과 화면을 볼 때 스크롤이 계속 초기화되는 문제를 줄이기 위해 목록 정렬/가상화/선택 상태를 보강했다.

### 8.6 KIE / Required checker / Classifier UI 정리

- KIE, 문서 분류, 필수 항목 확인 모듈의 업로드 버튼을 `업로드` 하나로 통합하고, 클릭 시 파일/폴더 업로드를 선택하는 패턴으로 맞췄다.
- KIE에서 이미지 사이드바가 두 개 보이던 문제를 수정했다.
  - 문서 목록 사이드바는 맨 왼쪽 하나만 남긴다.
- KIE 단일 이미지 업로드 시 좌측 목록/미리보기 레이아웃이 깨지는 문제를 수정했다.
- 홈 설정 모달은 글자가 늘어나도 행/열이 크게 깨지지 않도록 grid 구조를 정리했다.
- 홈 화면 hero 아래 01/02/03 가치 카드가 브라우저 zoom 변화에서 깨지던 문제를 보완했다.

### 8.7 Schema/Classifier/Checklist 복제

- 사용자가 “KIE schema, classifier class field, required checklist를 일괄적으로 스키마라고 부르겠다”고 정리했다.
- 기존 스키마를 복제하는 기능을 추가했다.
  - 이름은 기존 이름 뒤에 ` (1)`을 붙인다.
  - 예: `성명` -> `성명 (1)`
- KIE schema의 region, field 설정, `judgement_enabled` 값도 복제 시 보존한다.

### 8.8 KIE AI 검수 기능

- KIE field에 `judgement_enabled`를 추가했다.
  - UI 표기는 `AI 검수`
  - 기존 schema에는 값이 없으므로 기본값은 `false`
  - AI schema 추천으로 생성된 field도 기본 `false`
- KIE 1차 추출 후, `judgement_enabled=true`인 field만 2차 AI 검수를 수행한다.
- 2차 judgement 입력 텍스트에는 다음 정보를 포함한다.
  - `key_name`
  - `description`
  - 1차 판단근거/evidence
  - 1차 추론 결과
- region이 없는 경우와 region이 있는 경우의 judgement/correction prompt를 분리했다.
- region이 있는 경우 judgement/correction은 `full page + crop`을 사용한다.
  - crop은 이미 원본 페이지에서 지정된 영역이라는 점을 prompt에 명시한다.
  - description의 위치 표현을 crop 내부 좌표로 다시 해석하지 말라고 명시한다.
- judgement 출력은 `correct | needs_correction`만 허용한다.
  - `correct`: 1차 추론값 유지
  - `needs_correction`: correction 단계 실행 후 correction 결과를 최종값에 반영
- AI 검수/보정 실패 시 job 전체를 실패시키지 않고 기존 1차 값을 유지한다.
  - field warning에 `ai_review_failed` 또는 `ai_correction_failed`를 붙인다.
  - 결과 status는 `needs_review`로 둔다.
- KIE 결과 화면에 AI 검수 상태를 표시한다.
  - 검수 안함
  - 정상 판단
  - AI 보정
  - 검수 실패

### 8.9 프롬프트 계층 분리

- VLM prompt와 output schema를 `backend/app/prompts/` 아래로 분리했다.
  - `common.py`
  - `kie.py`
  - `classifier.py`
  - `required_checker.py`
  - `schema_recommendation.py`
- `backend/app/vlm.py`는 provider 호출, retry, multimodal 변환, credential 처리 중심으로 축소했다.
- KIE prompt는 다음 계층으로 나눴다.
  - 1차 추출 full page
  - 1차 추출 region
  - 2차 judgement full page
  - 2차 judgement region
  - 2차 correction full page
  - 2차 correction region
- Required checker prompt도 region 없음/있음 request를 분리했다.
  - region 없음: full page prompt/request
  - region 있음: region별 request, masked/crop 입력 사용
- Classifier와 schema recommendation prompt/output schema도 별도 모듈로 옮겼다.

### 8.10 KIE region 관련 확인 사항

- KIE에서 field에 region이 있으면 현재 입력은 다음과 같이 구성된다.
  - 1차 추출: full page + masked page + crop
  - 2차 judgement/correction: full page + crop
- crop은 전처리된 page image에서 잘라낸다.
  - 원본 파일을 직접 crop하는 구조는 아니다.
  - 현재 정책은 전처리 preview/VLM용 이미지를 적정 크기 JPEG로 만들고, 그 page 기준으로 crop한다.
- 이 구조는 현행 유지하기로 했다.
- UI에서 field에 region이 없는데 preview에 region이 보이는 문제를 확인했고, 실제 field에 연결된 region만 overlay에 보이도록 정리했다.

### 8.11 검증 상태

최근 통과한 검증:

```bash
.venv/bin/python -m pytest backend/tests/test_api.py -q
```

결과:

- backend API test `91 passed`

```bash
npm run build --prefix frontend
```

결과:

- frontend build 통과

```bash
git diff --check
```

결과:

- whitespace error 없음

브라우저 확인:

- 홈 화면 1280/980/760/520 폭에서 가치 카드 overflow가 없도록 확인했다.

주의:

- 실제 VLM 대량 smoke는 사용자 실행 중인 작업/비용 이슈 때문에 별도 자동 실행하지 않았다.
- VLM 동시 요청 수를 너무 높이면 DB pool, VLM provider rate limit, local CPU/IO 병목이 같이 커진다.

### 8.12 완료된 큐/보관함/동시성 작업

KIE 검수 프롬프트 보수화:

- judgment/correction prompt를 더 보수적으로 정리했다.
- 1차 추출값을 임의로 지우거나 새 값을 만들어내지 않도록 하고, 명백한 오류가 아니면 기존값을 유지하는 방향으로 수정했다.

워크플로우 실행 대기:

- `waiting` run과 `workflow_run_group_id`, `queued_from_run_id`, `queue_order` 기반 순차 실행을 구현했다.
- `실행 예약`은 업로드를 다시 하지 않고 기존 run의 document id만 재사용한다.
- 대기 삭제는 공유 문서 payload를 지우지 않고 대기 항목만 취소/삭제한다.
- 실행 현황은 우측 사이드바로 옮겼고, 상태별 색상과 export 통합 버튼을 정리했다.

문서 보관함 전환:

- 보관함이 원본 업로드와 변환을 담당한다.
- 모듈/워크플로우는 보관함에서 문서를 선택하거나, 새 업로드를 보관함에 넣은 뒤 실행한다.
- ready 문서는 즉시 실행하고, converting 문서는 `waiting_for_document`로 등록해 준비 완료 후 실행한다.

### 8.13 현재 git 상태 주의

현재 worktree에는 이번 세션의 큰 변경이 남아 있다. 문서 업데이트 후 commit/push 대상이다.

주요 modified/untracked 항목:

- `README.md`, `DEVELOPMENT_DEFINITION.md`, `ERROR_NOTE.md`, `인수인계.md`
- `backend/app/config.py`, `backend/app/concurrency.py`, `backend/app/database.py`
- `backend/app/document_modules.py`, `backend/app/extraction.py`, `backend/app/main.py`
- `backend/app/models.py`, `backend/app/schemas.py`, `backend/app/vlm.py`, `backend/app/workflows.py`
- `backend/tests/test_api.py`
- `frontend/src/App.tsx`, `frontend/src/DocumentLibrary.tsx`, `frontend/src/ModuleWorkspace.tsx`
- `frontend/src/WorkflowBuilder.tsx`, `frontend/src/styles.css`
- `backend/app/prompts/` 신규 디렉터리

사용자가 마지막에 `.gitignore`는 건드리지 말라고 명시했다. 이후 작업에서 `.gitignore` 수정은 피해야 한다.
