# Digitize Your Documents 인수인계

작성일: 2026-05-22  
브랜치: `main`  
최근 기준 커밋: `65675ac Polish module and workflow surfaces`

## 1. 프로젝트 한 줄 요약

`Digitize Your Documents`는 문서 접수부터 원문 변환, 문서 분류, 필수 항목 누락 확인, 핵심 정보 추출, 워크플로우 실행, CSV/JSON export까지 한 화면에서 처리하는 로컬 문서 자동화 워크스페이스다.

현재 제품 방향은 “문서 분석 데모”가 아니라 “업무자가 반복 접수/검수/취합 작업을 줄이는 도구”다.

## 2. 현재 구현 상태

현재 핵심 모듈은 모두 앱 안에 들어와 있다.

- Home
- Raw Data Extractor
- Key Information Extractor
- Document Classifier
- Required Field Checker
- Workflow Builder

최근 작업의 중심은 `Workflow Builder`였다. React Flow 기반 캔버스에서 모듈을 연결하고, classifier 결과에 따라 branch를 나누며, 각 문서의 실행 결과를 이미지 preview와 module별 table로 검수할 수 있게 만들었다.

## 3. 지금까지 완료한 주요 작업

### 3.1 VLM / batch 안정화

- VLM 호출 오류를 안정적인 `VLM_*` code로 정리했다.
- provider 오류 메시지에 API key가 섞이면 `[redacted]`로 마스킹하도록 정리했다.
- `VLM_PROVIDER=auto` 기준 provider 선택을 정리했다.
- batch 처리 중 DB session을 오래 잡지 않도록 구조를 정리했다.
- batch polling 500류 문제를 줄이기 위해 request/session lifecycle과 background worker를 분리하는 방향으로 보강했다.

중요 규칙:

- 실제 VLM 호출은 과금될 수 있으므로 사용자의 명시 승인 없이 실행하지 않는다.
- UI/기능 검증은 우선 `VLM_PROVIDER=mock` 또는 build/browser smoke test로 진행한다.

### 3.2 Document Classifier

- 문서 class 후보를 저장하고 불러오는 UI를 정리했다.
- `classified`, `unknown`, `needs_review` 상태를 지원한다.
- 단일/배치 업로드는 파일 개수로 자동 판단한다.
- UI 언어와 톤을 Required Field Checker/KIE 쪽과 맞췄다.
- Workflow branch의 기준 node로 사용할 수 있게 했다.

### 3.3 Required Field Checker

- 필수 항목 checklist를 저장하고 실행하는 모듈을 추가했다.
- 성명, 서명, 날짜, 체크박스, 도장 같은 항목의 존재 여부를 `present`, `missing`, `uncertain`, `not_applicable`로 판단한다.
- AI 추천 checklist 기능을 추가했다.
- batch 실행, 결과 table, export 흐름을 정리했다.

### 3.4 KIE / Raw

- KIE는 저장된 schema 기준으로 field/value/confidence/evidence를 추출한다.
- schema field별 optional region을 사용할 수 있다.
- Raw는 Office/PDF 원문을 preview와 HTML로 변환하는 모듈이다.
- Office preview는 LibreOffice headless 변환 경로를 사용한다.

### 3.5 Workflow Builder

구현된 v1 node:

- `Input`
- `Document Classifier`
- `Branch`
- `KIE`
- `Required Field Checker`
- `Merge`
- `Export`

주요 동작:

- Home에서 Workflow Builder 진입 가능.
- React Flow canvas에서 node 추가, 이동, 연결 가능.
- 우측 node 설정 sidebar는 접기/펼치기 가능.
- workflow draft는 localStorage에 자동 저장되어 나갔다 들어와도 복원된다.
- 저장된 schema/classifier/checklist를 node 설정에서 선택한다.
- 새 항목이 필요하면 기존 KIE/Classifier/Required 화면으로 이동하는 보조 동선을 제공한다.
- 업로드 파일이 1개면 단일, 2개 이상이면 batch로 자동 판단한다.
- classifier 결과에 따라 class별 branch path를 실행한다.
- class별로 다른 KIE schema와 Required checklist를 연결할 수 있다.
- 후속 node가 없는 class/fallback은 오류가 아니라 “분류 결과까지만 export”로 처리한다.
- branch fallback 누락은 warning으로 보여주되 저장/실행을 막지 않는다.
- 연결선 삭제 기능을 제공한다.
- 한 후속 node에 여러 class가 연결될 때 확인 팝업을 띄운다.

### 3.6 Workflow 실행 결과 UX

기존의 “캔버스 아래에 결과가 좁게 붙는 구조”가 불편해서 결과 검수 화면을 분리했다.

- `결과만 보기` 버튼으로 캔버스를 접고 결과에 집중할 수 있다.
- 좌측에는 문서 rail을 둔다.
- 중앙에는 선택 문서 이미지 preview를 둔다.
- 우측 inspector에는 classification, branch, 현재 node, KIE table, Required table을 보여준다.
- running/queued/completed/needs_review 상태를 문서 단위로 확인할 수 있다.
- backend `WorkflowRunItem.result_json`에 부분 진행 정보가 저장된다.

진행 정보 필드:

- `current_node_id`
- `current_node_kind`
- `current_node_label`
- `completed_node_ids`
- `path_node_ids`
- `node_results`

### 3.7 Workflow 병렬 처리

- workflow batch item 처리를 `ThreadPoolExecutor`와 `BATCH_MAX_WORKERS` 기준으로 병렬화했다.
- worker마다 독립 DB session을 사용한다.
- 한 문서가 실패해도 나머지 문서는 계속 처리한다.
- run status는 item들이 terminal 상태가 된 뒤 finalize한다.
- 병렬 처리 설명 보고서는 `reports/workflow_parallel_before_after.html`에 있다.

### 3.8 Home / 전체 디자인 개편

- Home은 Workflow Builder를 대표 기능으로 크게 보여주는 구조로 바꿨다.
- 단일 모듈들은 “핵심 기능” 카드로 묶었다.
- Toss Design System 문서를 참고해 회색 표면, blue primary, 명확한 카드/리스트 구조를 적용했다.
- 이후 같은 스타일을 KIE, Classifier, Required Checker, Workflow Builder에도 공통 CSS로 확장했다.

최근 디자인 관련 커밋:

- `2c19109 Apply Toss-inspired UI polish`
- `1c36323 Add README visual artboards`
- `65675ac Polish module and workflow surfaces`

## 4. 주요 파일 안내

Backend:

- `backend/app/main.py`: FastAPI API entrypoint. 문서, schema, classifier, checklist, workflow API가 모여 있다.
- `backend/app/models.py`: SQLAlchemy model. workflow definition/run/run item 모델 포함.
- `backend/app/schemas.py`: request/response schema.
- `backend/app/vlm.py`: VLM provider 선택, prompt 호출, 오류 정규화.
- `backend/app/workflows.py`: workflow validation, branch 결정, run engine, export.
- `backend/tests/test_api.py`: API와 workflow 회귀 테스트.

Frontend:

- `frontend/src/App.tsx`: app mode, Home, KIE/Raw 주요 UI.
- `frontend/src/ModuleWorkspace.tsx`: Classifier와 Required Checker 공통 workspace.
- `frontend/src/WorkflowBuilder.tsx`: React Flow workflow builder, branch 연결, run result workbench.
- `frontend/src/styles.css`: 전역 디자인 토큰과 모듈/workflow 스타일.

Docs/assets:

- `README.md`: 제품 소개, 가치, 기능, 설치/실행, VLM 설정.
- `DEVELOPMENT_DEFINITION.md`: 제품/개발 기준.
- `ERROR_NOTE.md`: 주요 오류와 해결 기록.
- `reports/workflow_parallel_before_after.html`: workflow 병렬화 before/after 보고서.
- `assets/readme-src/*.html`: README용 이미지 아트보드 source.
- `assets/readme/*.png`: README 삽입용 캡처 이미지.

## 5. 실행 방법

Backend와 frontend를 함께 실행:

```bash
./scripts/run_dev.sh
```

Frontend build:

```bash
cd frontend
npm run build
```

Backend test:

```bash
.venv/bin/python -m pytest backend -q
```

실제 VLM 호출 없이 확인:

```env
VLM_PROVIDER=mock
```

Gemini native 예시:

```env
VLM_PROVIDER=auto
VLM_API_KEY=AIza...
VLM_MODEL_NAME=gemini-3.1-flash-lite
VLM_BASE_URL=
```

OpenAI-compatible gateway 예시:

```env
VLM_PROVIDER=auto
VLM_API_KEY=...
VLM_MODEL_NAME=google/gemini-3.1-flash-lite
VLM_BASE_URL=https://openrouter.ai/api/v1
```

## 6. 검증 기록

최근 검증:

- `frontend`에서 `npm run build` 통과.
- Home, KIE, Classifier, Required Checker, Workflow Builder 화면을 headless Chrome screenshot으로 확인.
- Workflow result focused 화면에서 문서 rail, preview, inspector table 레이아웃 확인.
- Workflow branch 연결, autosave, 결과 focused view, batch 병렬 처리 관련 테스트가 추가되어 있다.

주의:

- Vite build에서 JS chunk가 500 kB를 넘는다는 warning이 있다. 기능 실패는 아니지만 추후 code splitting 후보다.
- 실제 VLM 소량 smoke test는 과금 가능성이 있으므로 매 호출 전 사용자 승인 필요.

## 7. 현재 worktree 주의사항

이 문서를 작성하기 전부터 아래 파일에는 미커밋 변경이 남아 있었다.

- `.gitignore`
- `DEVELOPMENT_DEFINITION.md`
- `ERROR_NOTE.md`
- `assets/readme_overview.png`

이번 인수인계 작업에서는 위 파일들을 staging하지 않고, `인수인계.md`만 별도 커밋/푸시 대상으로 다룬다.

## 8. 남은 개선 후보

우선순위 높은 후보:

- 실제 VLM 호출을 사용자 승인 후 소량으로 smoke test한다.
- README 이미지 품질을 다시 정식으로 손본다. 현재 README용 아트보드는 들어갔지만, 사용자가 이미지 품질에 아쉬움을 표현했다.
- Workflow result view를 modal/standalone review 화면으로 더 발전시킬 수 있다.
- Workflow run cancel API, websocket/SSE progress, workflow version/publish를 후속으로 설계한다.
- XLSX export를 추가한다.
- branch 조건을 classifier class 외 임의 조건식으로 확장한다.
- frontend bundle code splitting을 검토한다.

## 9. 작업할 때 지켜야 할 기준

- 실제 VLM 호출 전에는 사용자에게 반드시 묻는다.
- 사용자가 만든 미커밋 변경은 되돌리지 않는다.
- schema/classifier/checklist는 workflow 안에서 깊게 편집하지 않고, 저장된 라이브러리 선택을 기본으로 한다.
- 단일/배치는 사용자가 고르는 것이 아니라 업로드 파일 개수로 판단한다.
- fallback/branch path 누락은 warning으로 보여주되 가능한 한 저장과 실행을 막지 않는다.
- 결과는 table로 검수하기 쉽게 보여주고 CSV/JSON export 가능성을 유지한다.
- 디자인은 Home에서 정리한 Toss-inspired 톤을 기준으로 각 모듈까지 일관되게 맞춘다.
